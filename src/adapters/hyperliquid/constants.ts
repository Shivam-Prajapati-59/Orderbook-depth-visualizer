// Hyperliquid WebSocket endpoint for real-time data
export const WS_URL = 'wss://api.hyperliquid.xyz/ws';
// Hyperliquid REST API endpoint for info/metadata queries
export const INFO_API_URL = 'https://api.hyperliquid.xyz/info';
// Interval (ms) between keepalive pings on the WebSocket connection
export const PING_INTERVAL_MS = 20_000;